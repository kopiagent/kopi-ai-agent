#!/usr/bin/env python3
"""
Kopi Agent CLI launcher.

This wrapper should behave like the installed `kopi` command, including
subcommands such as `gateway`, `cron`, and `doctor`.
"""

if __name__ == "__main__":
    from kopi_cli.main import main
    main()

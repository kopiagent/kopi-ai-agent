name: kopi-proxy
kind: model-provider
version: "1.1"
description: "KOPI official gateway — the default AI model provider for KOPI AI AGENT. Routes kopi-* models through bill.kopiagent.ai (Kopi TokenMax) with per-key quota and billing. Registers the provider under the canonical name 'kopi' ('kopi-proxy' remains an alias)."
